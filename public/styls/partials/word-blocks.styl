
.word-blocks
    line-height 1
    //position relative
    //display flex
    flex-direction column
    align-items center  
    gap .2rem
    margin 0
    width 100%

.word-block
    text-transform: uppercase
    font-size 5.7rem
    font-weight bold
    padding  5px
    color alpha - 75%
    border none //4px solid
    margin-bottom 2px
    flex 1
    //border-radius 30%
    //filter blur(1px)
.word-block-1
    //color alpha
    //background alpha - 40%
    border-color alpha + 20%
    background alpha //linear-gradient(90deg, rgba(alpha,.01) 30%, rgba(alpha,.6))
    //border-right 4px solid alpha + 10%

.word-block-2
    //color beta
    background beta - 40%

    //background beta //linear-gradient(90deg, rgba(beta,.01), rgba(beta,.5))
    //border-right 4px solid beta
.word-block-3
    background gamma //- 40%
    border-color gamma //+ 20%
    //color gamma
.word-block-4
    border-color delta 
    background delta
.word-block-5
    //color epsilon
    background epsilon - 40%
    border-color epsilon + 20%

.word-block.md-block
    font-size 3rem
.blocks-wrap
    display flex
    //flex-direction column
    align-items center
    justify-content center
    gap .5rem
    margin-bottom .5rem
    //border 1px solid white